#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int *a = new int[n];
    
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    
    int bottom = 0;
    int top = n - 1;
    
    while (bottom <= top)
    {
        if( a[bottom] == 0 )
        {
            for(int j = bottom; j < top; j++)
            {
                a[j] = a[j+1];
            }
            top--;
        }
        else
        {
            bottom++;
        }
    }
    
    
    for(int i = 0; i <= top; i++)
    {
        cout << a[i] << " ";
    }
    cout << endl;
    
    delete[] a;
    return 0;
}
