// project name : 数据清洗
// file name : main.cpp

#include <iostream>
#include "Node.h"
using namespace std;


int main()
{
    Node *head;
    int n;
    cin >> n;
    Node *number = new Node[n];
    
    for(int i = 0; i < n; i++)
    {
        cin >> number[i].value;
    }
    
    head = &number[0];
    *head.value = 1;
    
    for(int i = 0; i < n - 1; i++)
    {
        a[i].next = & a[i+1];
    }
    
    a[n-1].next = NULL;
    
    
    // start clear the data
    while( *(*head.next).value == 0 )
    {
        *(head).next = *(*head.next).next;
    }
    
    Node *point;
    point = head;
    while( point != NULL )
    {
        if( *point.value == 0 )
        {
            
        }
    }
    
    
    return 0;
}

// end 
// ism 

